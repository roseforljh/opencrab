/**
 * 这个文件用于定义 Next.js 的基础运行配置。
 *
 * 当前阶段只保留最小配置，原因是项目还处于骨架搭建期，
 * 先保证能稳定启动，再逐步补充更细的构建、图片、实验特性配置。
 */
const nextConfig = {
  reactStrictMode: true,
  output: "standalone"
};

export default nextConfig;
