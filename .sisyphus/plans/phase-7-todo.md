# 阶段 7 TODO：部署与验收

## 阶段目标

确保这个项目真的能被个人部署起来，而不是只停留在开发环境中。

## 待办清单

- [x] 编写 Dockerfile。
- [x] 编写 `docker-compose.yml`。
- [x] 提供默认配置样例。
- [x] 提供 SQLite 数据挂载方案。
- [x] 编写部署文档。
- [x] 编写常见问题文档。
- [x] 编写首版验收清单。
- [x] 验证本地运行。
- [x] 验证 Docker 运行。

## 完成标准

1. 本地部署成功。
2. Docker 部署成功。
3. 文档足以指导别人完成部署。

## 验证清单

1. 本地按文档执行启动命令，预期服务正常启动。
2. 本地访问管理台和健康检查接口，预期都能正常返回。
3. 执行 Docker 构建，预期镜像构建成功。
4. 执行 `docker compose up`，预期容器正常运行。
5. 容器启动后再次请求 `/healthz` 与管理台页面，预期都能正常访问。
6. 按部署文档从零执行一遍，确认文档没有缺步骤。
